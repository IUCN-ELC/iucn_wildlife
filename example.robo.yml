project:
  version: "2.2"
  sites:
    default:
      default_dump_location: web/sync/database.sql
      sync:
        username: username
        password: password
        sql:
          url: https://www.wildlex.org/sync/database.sql.gz
        files:
          url: https://srv.storage.com/sync/files.tar.gz
      develop:
        admin_username: user@mail.com
        config_split: FALSE
        modules:
          - devel
