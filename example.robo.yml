version: "3.0"

sites:
  default:
    sync:
      username: username
      password: password
    sql:
      dump:
        location: web/sync/database.sql
      sync:
        source: https://www.wildlex.org/sync/database.sql.gz
    files:
      dump:
        location: web/sync/files.tar.gz
      sync:
        source: https://www.wildlex.org/sync/files.tar.gz
    site:
      update:
        excluded_commands: []
        extra_commands : []
      develop:
        admin_username: username
        config_split: FALSE
        modules: []